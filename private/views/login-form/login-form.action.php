<?php
/*
if ($mainView === 'login-page') {
  
  if ($_SERVER['HTTP_REFERER'] !== WEBSITE_BASE_URL . VIEWS['registration-page']['meta']['url']) {
    
    // if last page is not registration page, redirect to last page
    
    $redirectUrl = $_SERVER['HTTP_REFERER'];
    
  } else {
    
    // if last page is registration page, redirect to home page
    
    $redirectUrl = WEBSITE_BASE_URL;
  }
  
} else {
  
  // redirect to last page after login
  
  $redirectUrl = WEBSITE_BASE_URL . preg_replace('/^\//', '', $_SERVER['REQUEST_URI']);
}*/

?>
