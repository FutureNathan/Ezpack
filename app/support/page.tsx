import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function SupportPage() {
  return (
    <PlaceholderScreen
      title="Support"
      note="Help and contact options aren't built in this prototype. This tab is a placeholder from the mock."
    />
  );
}
